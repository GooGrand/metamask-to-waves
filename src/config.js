const headers = {
        'Accept': 'application/json',
        'X-API-Key': 'superb',
        'Content-Type': 'application/json'
    };
const nodeUrl  = "http://127.0.0.1:6869"
const nodeTestnetUrl = 'https://nodes-testnet.wavesnodes.com';
const distributorSeed = 'liberty toast flower vivid park gain sketch eagle image develop april still pumpkin swap sniff';

module.exports = {
    distributorSeed,
    headers,
    nodeTestnetUrl,
    nodeUrl
}
